sudo: false
dist: trusty
language: python
python:
  - "3.6"
env:
  global:
    - GIT_ASK_YESNO='false'
    - PYTHONIOENCODING='utf-8'

install:
  - "pip install --upgrade pip"
  - "pipenv install --dev"
  - 'pipenv install --system --dev'

script:
  - 'pipenv run time pytest -v'
